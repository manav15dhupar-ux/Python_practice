# If the names of 2 friends are same; what will happen to the program in problem
# 6?

fav={}

name=input("Eneter the name:")
language=input("Eneter the language:")
fav.update({name:language})

name=input("Eneter the name:")
language=input("Eneter the language:")
fav.update({name:language})

name=input("Eneter the name:")
language=input("Eneter the language:")
fav.update({name:language})

name=input("Eneter the name:")
language=input("Eneter the language:")
fav.update({name:language})

print(fav)


# update method will take the last update as a output and print it.

# for values it will skip 1st position
